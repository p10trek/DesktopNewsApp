<UserControl x:Class="NewsApp.WPF.Components.QSearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewsApp.WPF.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="1.2*"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0"  Grid.Row="0" FontSize="20">Search Query:</Label>
        <TextBox Grid.Column="1"  Grid.Row="0" Height="30" TextAlignment="Center" Padding="3" FontSize="15" VerticalAlignment="Center"/>

        <CheckBox Grid.Column="1"  Grid.Row="1" FontSize="15" VerticalAlignment="Center" IsChecked ="{Binding IsCustomSettings, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Grid.Column="0"  Grid.Row="1" FontSize="20" Content="Use saved preferences"/>
        
        <Button Grid.RowSpan="2"  Content="Search" Grid.Column="3"/>
    </Grid>
</UserControl>
