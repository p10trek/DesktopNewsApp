<UserControl x:Class="NewsApp.WPF.Components.MenuStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewsApp.WPF.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Command="{Binding ShowPreferencesCommand }">Preferences</Button>
        <Button Grid.Column="1">Quick Search</Button>
        <Button Grid.Column="2">Advance Search</Button>
        <Button Grid.Column="3">Favorites</Button>
        <Button Grid.Column="4">History</Button>
        <Button Grid.Column="5">Login/Register</Button>
    </Grid>
</UserControl>
